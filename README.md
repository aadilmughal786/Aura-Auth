# Aura Auth

Privacy-first, client-side TOTP 2FA generator.

## Features
- Zero server dependencies
- AES-encrypted local storage
- Real-time pulse synchronization
- Industry-standard TOTP codes

## Usage
Open `index.html` in a browser, add accounts using their Base32 secret keys.
